{
  config.vim = {
    formatter.conform-nvim.enable = true;

    statusline.lualine.enable = true;

    utility = {
      motion = {
        flash-nvim = {
          enable = true;
        };
      };
    };
  };
}
